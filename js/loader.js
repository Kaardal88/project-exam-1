/* export function showSpinner(count) {
    container.innerHTML = '';
    for (let i = 0; i < count; i++) {
        const loaderContainer = document.createElement('div');
        loaderContainer.className = 'loader-container';
        container.appendChild(loaderContainer);
    }
} */